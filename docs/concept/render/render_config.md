# Render configuration

The `render.conf` store each individual settings shared by each `IRenderer` here is the list :

- `cacheCompiledShaders` (bool) : Store compiled shaders into a `.cache/shader/` folder to optimize compilation time.